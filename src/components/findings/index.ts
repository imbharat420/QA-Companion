/**
 * The /findings composites. The page imports from here, never from a file
 * inside this directory.
 */

export { FindingRow, type FindingRowProps } from "./FindingRow";
export { FindingDetail, type FindingDetailProps } from "./FindingDetail";
