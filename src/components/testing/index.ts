/**
 * The /suites + /cases composites. Both pages import from here, never from a
 * file inside — same contract as components/shared.
 */

export { CaseDetail, type CaseDetailProps } from "./CaseDetail";
export { SuiteTree, type SuiteTreeProps } from "./SuiteTree";
